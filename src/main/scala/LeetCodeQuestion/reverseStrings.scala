package LeetCodeQuestion

object reverseStrings extends App {

/*  def reverseVowels(s: String): String = {
    val s = "hello"
    val string_list = s.toList
    val vowel_list = List('a','e','i', 'o', 'u')
    val newList = s.filter(vowel_list.contains).reverse.toList
    println(newList)

    def recFunc(str: String, accumulator: String, l:Int) : String ={
      if(l > s.length) accumulator
      else recFunc(str, if(string_list.contains(newList))
    }*/











  }


